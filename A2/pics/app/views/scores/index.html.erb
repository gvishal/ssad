<h1>Scores#index</h1>

<h2>Leaderboard</h2>

<% if @scores %>
  <table>
    <tr>
      <th>Name</th>
      <th>Score</th>
    </tr>
  <% @scores.each do |score| %>
    <tr>
      <td><%= score.user.name %></td>
      <td><%= score.score %></td>
    </tr>
  <% end %>
  </table>
<% end %>